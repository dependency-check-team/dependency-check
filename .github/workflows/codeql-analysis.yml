name: "CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '24 46 * * 4'

jobs:
  analyze:
    uses: voxpelli/ghatemplates/.github/workflows/codeql-analysis.yml@main
