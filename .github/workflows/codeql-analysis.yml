name: "CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '26 8 * * 4'

jobs:
  analyze:
    uses: voxpelli/ghatemplates/.github/workflows/codeql-analysis.yml@main
